FROM python:3.11-slim

RUN apt-get update && apt-get install -y \
    ffmpeg \
    && rm -rf /var/lib/apt/lists/*

RUN pip install --no-cache-dir pymysql

WORKDIR /app

COPY clip_watcher.py .

RUN mkdir -p /app/nas/clips

CMD ["python3", "-u", "clip_watcher.py"]
